
<div class='container'>
  <div class='row'>
    <h3 class="module-title"><%= "Coffee's from" + ' ' + @shop.name %></h3>
    <p><%= link_to 'Back to list', shops_path %></p>
    <% @shop.items.each do |item| %>
    <div class="col-sm-3">
      <div class="menu-classic">
        <figure class="overlay">
          <%= image_tag('cappucino.jpg') %>
            <div class="col-sm-9">
              <h4 class='menu-title'><%= item.name %></h4>
            </div>
            <div class="col-sm-3">
              <h4 class='menu-price'><%= item.price.to_s + '€' %></h4>
            </div>
        </figure>
          <div>
            <h4><%= link_to 'Add', add_to_cart_shop_path(@shop, item_id: item.id) %></h4>
          </div>
      </div>
    </div>
    <% end %>
    <h4><%= link_to 'Check my cart', cart_shop_path(@shop.id) %></h4>
  </div>


  </br>
  </br>

  <div class="row">
    <div class="col-sm-4">
    <h3 class="module-title"><%= @shop.name %></h3>
    <p class='menu-detail'><%= @shop.address %></p>
    </div>
    <div class="col-sm-8">
      <div id="map">
      <h4>Placeholder map</h4>
      </div>
    </div>
  </div>

</div>

